extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-projects
    .inner-wrapper.flex-row-wrap.two-col
      .project-info-box.box


        h1= project.project_name

        //- Use interpolation to dynamically add the project description
        p= project.description

      .project-tech-links-box.box
        h6 Technologies
        ul
        each technology in project.technologies
          li= technology

        //- Add the href values to the links below
        a.btn-link(href= project.live_link) Live Demo
        a.btn-link(href= project.github_link) GitHub Repo

    .project-img-box.box
      
    each image in project.image_urls
      img.project-img(style='max-width: 700px; max-height: 700px;', src=image, alt='')
    
    nav.nav
      a(href='/')
        span &larr;
        |  Back
